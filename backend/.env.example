# FastFlix Backend - Environment Variables Example
# Copy this file to .env.local and fill in your actual values
#
# Legend:
#   [REQUIRED] - Must be set in all environments
#   [PROD]     - Required in production only
#   [OPTIONAL] - Not required

# =============================================================================
# DATABASE [REQUIRED]
# =============================================================================
# Turso SQLite database - https://turso.tech/
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your_turso_token

# =============================================================================
# AI & DATA [REQUIRED]
# =============================================================================
GOOGLE_API_KEY=your_google_ai_key
GEMINI_MODEL=gemini-2.5-flash
TMDB_API_KEY=your_tmdb_key
TMDB_BASE_URL=https://api.themoviedb.org/3

# =============================================================================
# RATE LIMITING [OPTIONAL]
# =============================================================================
# Per IP limits (requests per minute)
RATE_LIMIT_IP_SEARCH=10
RATE_LIMIT_IP_CHECK_LIMIT=30
RATE_LIMIT_IP_WEBHOOK=100
# Per device limits (requests per minute)
RATE_LIMIT_DEVICE_SEARCH=5
RATE_LIMIT_DEVICE_CHECK_LIMIT=20

# =============================================================================
# REVENUECAT [PROD]
# =============================================================================
# Get this from RevenueCat Dashboard > Integrations > Webhooks
# WARNING: REQUIRED in production! Webhooks will be rejected without this.
REVENUECAT_WEBHOOK_SECRET=your_webhook_secret_key

# =============================================================================
# AUTHENTICATION [REQUIRED]
# =============================================================================
# Generate a secure random string for JWT_SECRET (at least 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_in_production_minimum_32_chars

# Apple Sign In - Get from Apple Developer Console > Identifiers > App IDs
APPLE_CLIENT_ID=com.fastflix.app

# Google Sign In - Get from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_ios_client_id.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID=your_google_android_client_id.apps.googleusercontent.com

# =============================================================================
# SENTRY [OPTIONAL]
# =============================================================================
# Get these from Sentry Dashboard > Settings > Auth Tokens
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# =============================================================================
# CONFIGURATION
# =============================================================================
# Set to 'production' for production deployments
# Vercel automatically sets VERCEL_ENV
NODE_ENV=development
